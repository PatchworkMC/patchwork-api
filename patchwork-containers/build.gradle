archivesBaseName = "patchwork-containers"
version = getSubprojectVersion(project, "0.1.0")

dependencies {
	compile project(path: ':patchwork-capabilities', configuration: 'dev')
}

minecraft {
	accessWidener "src/main/resources/patchwork-containers.accesswidener"
}
